> The semantic tag give more meaning to code like <article>,<section>,<main>.
> The semantic tag are useful when we add class to semantic tags to avoid choas
> only use divs where it is very important to use to avoid litter.

